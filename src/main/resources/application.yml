server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/WebCrew-Funda/config-data.git
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
          default-label: "main"
          searchPaths: users-service,payments-service
          cloneOnStart: true
